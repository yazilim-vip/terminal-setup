# terminal-setup — Claude Code entrypoint

This is a thin bridge. The canonical, tool-neutral operating manual is **`AGENTS.md`** (Claude Code
does not read `AGENTS.md` natively yet, so this file imports it). Read and follow it:

@AGENTS.md
